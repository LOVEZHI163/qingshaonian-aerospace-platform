{
  "name": "qingshaonian-aerospace-platform",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/web",
    "apps/admin",
    "apps/api"
  ],
  "scripts": {
    "dev": "concurrently -n api,web,admin -c blue,green,magenta \"npm run dev -w apps/api\" \"npm run dev -w apps/web\" \"npm run dev -w apps/admin\"",
    "build": "npm run build -w apps/web && npm run build -w apps/admin",
    "start:api": "npm run start -w apps/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
