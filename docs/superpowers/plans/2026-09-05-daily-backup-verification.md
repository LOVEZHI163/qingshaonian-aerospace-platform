# 每日数据库与附件备份补齐验收

用户于 2026-09-05 确认将附件纳入每日任务并立即补做备份。

## 实施

- `deploy/backup-postgres.sh` 每轮先生成数据库 dump，再调用既有 `backup-uploads.sh` 生成并校验附件压缩包。
- 开始备份时清除旧成功标记，数据库与附件均成功后才更新标记；失败返回非零，由现有容器重启策略重试。
- 周期仍为 86400 秒，保留原有约 7 天清理规则。本次未删除任何过期备份。
- 仅重启 backup 服务，未重启网站/API/数据库，未修改业务数据及短信配置。
- 部署脚本提交：`e9b08e67a5cc32607a95bdb40829c7dc658ce463`。网站运行版本仍为 `6245bc5c497e4bb5ced83b3841a6cc33fa987819`，本次为运维脚本更新，不需要重建应用。
- 原脚本备份：`/root/aerogp-backup-cycle-20260905/original-backup-postgres.sh`。

## 验证

隔离脚本测试：修复前仅产生数据库备份，缺少附件导致测试失败；修复后普通文件备份和内容恢复通过。包含不安全软链接的附件被拒绝，任务返回失败且没有成功标记。测试中的数据库命令使用合成输出，附件打包、校验及脚本编排为真实执行。

正式备份于北京时间 2026-09-05 12:23:57 生成：

- 数据库：`/opt/aerogp/backups/aerogp-20260905T042357Z.dump`
- 附件：`/opt/aerogp/backups/uploads/aerogp-uploads-20260905T042357Z-ioFANI.tar.gz`

新备份在 network=none、无外部端口、临时文件系统的独立容器中实际恢复。数据库恢复成功；附件恢复 226 个文件；数据库中 49 个未清理文件引用全部匹配，缺失 0 个。因此上一轮缺少的 3 个引用现已覆盖。未输出姓名、身份证或文件内容。

临时恢复容器及临时数据已自动清理，源备份保留。API/Web/PostgreSQL/backup 健康，公网版本接口 HTTP 200；backup 周期环境变量为 86400。

## 边界

这是同服务器备份，不是异地灾备。数据库快照与在线附件复制不是一个原子快照；本次所选备份已核对 49 个文件引用完整，不代表所有未来备份或历史快照已完成恢复演练。
