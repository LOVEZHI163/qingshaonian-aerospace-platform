# 报名提示、年份与证书一致性检查

## 用户确认范围

- 删除历届赛事独立年份标签；暂不改多赛事行为。
- 明确报名提交和审核状态，提供查看报名记录入口。
- 检查资料修改与证书一致性；复现个人修改入口缺少名单锁后，用户确认一起修复。
- 隔离恢复演练，不覆盖正式数据库。
- 用户明确人机验证保持关闭，不新增免注册领证入口。

## 修改与验证

- 首页已有不显示年份标签的回归覆盖；历史回顾页删除以发布日期生成年份的标签，标题和正文不变。
- 报名成功提示读取服务端实际状态，不把提交成功当作审核通过。个人及组织入口都有查看记录按钮，成功使用绿色提示。
- 个人修改接口原先在已有证书后仍允许修改学校等资料（本地合成账号复现 HTTP 200），现复用已有成绩/证书名单锁。姓名、学校、年级、手机号、身份证变化被拒绝为 HTTP 409，不写入报名、身份或证书表。
- 全量 Admin 662 项、Web 213 项通过；后续提示样式调整后相关 Admin 83 项及构建再次通过。
- 报名、证书、组织证书历史相关 API 51 项通过，含个人、组织、管理员入口及团队队员证书约束。
- 手机 390px 浏览器验证：仅提交一次、显示等待审核而不是已通过、查看记录按钮有效、无横向溢出。API 全部模拟，不发送短信或创建真实报名。agent-browser CLI 不可用，使用已安装的 Playwright/Chrome 验证。

## 备份演练

恢复容器使用 network=none、无对外端口，数据放临时文件系统；备份以只读方式挂载。未连接正式数据库。

- 数据库备份：`aerogp-20260904T053413Z.dump`。
- 恢复成功：34 张表；15 个账号、1 场赛事、17 条报名、5 份证书记录（仅统计，未输出身份资料）。
- 附件备份：`aerogp-uploads-20260902T104126Z-cHbAAH.tar.gz`，安全路径检查及实际解压通过，221 个文件，约 25.5 MiB。
- 数据库中 49 个未清理文件引用，3 个未包含于上述附件备份；3 个目前均仍存在于正式文件卷，因此是备份滞后，不是线上文件丢失。
- 现有 backup-postgres.sh 每日只备份数据库，root crontab 未发现附件备份任务。已向用户提出把附件加入每日任务的建议，未在未批准前修改调度。

此演练证明所选备份能读取并恢复，但当前较早的附件备份不能完整覆盖较新的数据库引用。也不等于已验证异地灾备或所有历史快照。

后续更新：用户已确认补齐每日附件备份，实施及新备份恢复验收见 `2026-09-05-daily-backup-verification.md`；新的 49 个文件引用全部匹配，缺失 0 个。下方“尚待确认”是前一阶段的历史状态。

## 线上验收

已部署到 server115，运行版本 `6245bc5c497e4bb5ced83b3841a6cc33fa987819`。API 版本、服务器 `.release` 和环境变量 RELEASE_SHA 一致。API、Web、数据库、备份容器健康。回滚文件保留在 `/root/aerogp-registration-safety-20260905/backup`，旧 API/Web 镜像标签为 `rollback-registration-safety-20260905`。

真实官网历史页已确认有回顾卡片且不再渲染年份标签，手机无横向溢出；线上静态资源搭配模拟认证接口的提交提示和记录跳转验证通过。线上短信注册、登录、密码重置仍开启，人机验证仍关闭。

演练临时容器 `aerogp-restore-check-20260905` 经专用标签及 network=none 核对后已删除，临时数据库和解压附件随 tmpfs 清理，原始备份未删除。附件每日备份及补做备份的请求尚待用户确认，未改调度。
