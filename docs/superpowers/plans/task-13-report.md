# Task 13：多赛事账户与组织体系部署报告

日期：2026-07-30
状态：通过并已部署

## 发布内容

- 远端发布版本：`b7520aab197b94dfa0d4ba043437311956ff1975`。
- 该版本同步了多赛事账户、组织和报名上下文的最终实现，并修正了普通用户报名页对非普通用户的旧报名接口回退。
- 部署冒烟脚本改为动态选择已发布且未归档的赛事，验证事件范围内的管理员报名接口，同时确认已移除的旧管理员报名路由返回 `404`。

## 本地发布门禁

- API：41 个测试文件逐文件/分组全量通过；其中组织凭据和公开站点部署聚焦复验 35 项通过。
- 管理端：34 个测试文件、307 项通过；报名页赛事上下文聚焦测试 3 项通过。
- 官网：6 个测试文件、133 项通过。
- 生产构建、配置校验、Compose 配置校验和 `git diff --check` 均通过。

## 备份与可回滚性

- 数据库：`/backups/aerogp-20260730T054309Z.dump`（备份程序已做恢复列表验证）。
- 上传卷：`/backups/uploads/aerogp-uploads-20260730T054316Z-eeAEFH.tar.gz`（备份程序已验证）。
- 旧源码：`backups/source-before-multi-event-20260730T054329Z.tgz`。
- 回滚镜像：`aerogp-api:rollback-20260730T054329Z`、`aerogp-web:rollback-20260730T054329Z`；标记文件已复核。
- 部署期间没有执行 `docker compose down -v`，没有删除数据库卷或上传卷。

## 清理与管理员初始化

用户已明确确认线上只有测试数据。清理先预览，再用精确确认串执行。保留赛事、赛项、项目组、站点设置、公开赛事资料、内容和媒体；测试组织、成员、报名、结果、证书、导入批次、审计、会话等均被清除，1 个测试上传文件已删除且无失败。

随后通过 stdin 创建一个新的赛事管理员。凭据仅位于服务器的权限 `0600` 文件 `/root/aerogp-admin-credentials.txt`，包含姓名、手机和密码三行；密码未打印、未提交、未写入文档。

终态计数：用户 1，组织 0，成员 0，赛事参与 0，报名 0，结果 0，证书 0，导入批次 0，审计日志 0；赛事 4，赛项 12，内容 3。

## 远端构建与验收

- `docker compose build --pull` 和 `docker compose up -d --wait --wait-timeout 240` 成功。
- API、PostgreSQL、Web、Backup 全为 `healthy`；仅 Web 映射宿主机 80 端口。
- 远端 smoke 全通过：公开首页、赛事、内容、sitemap、品牌资源、管理员登录、管理员赛事、账户赛事、按赛事管理员报名、已认证设置/内容均为 `200`；旧管理员报名路由为预期 `404`，匿名设置为预期 `401`。
- Chrome 实测：桌面 1920px 与 360px 均无横向溢出，控制台 error 均为 0；移动菜单中的唯一“用户登录”入口能进入 `/admin/`，后台登录页 360px 同样无溢出与错误。

## 角色与赛事隔离补充验收

- 临时创建普通用户和带有效 PNG 资质的组织负责人，所有临时凭据只在浏览器运行时内生成和使用，未回显；组织通过真实管理员审核接口审核为已通过。
- 真实 Chrome 中，普通用户登录后赛事中心显示 2 个可报名赛事。为第一个赛事建立一条临时报名后，浏览器赛事卡片显示第一个赛事 1 条、第二个赛事 0 条；按赛事接口也分别返回 1 条和 0 条，证明切换赛事时不串数据。
- 真实 Chrome 中，已审核组织负责人登录后同样看到 2 个“可加入”赛事。点击第一个“加入赛事”即进入对应赛事工作台；返回中心后第一个显示已加入、第二个仍可加入，加入第二个后进入第二个工作台。两个工作台的报名、待审核、证书均为 0，没有混入普通用户的个人报名。
- 组织工作台仅展示组织报名、报名记录、成绩、证书四个业务标签；组织导航为赛事中心、赛事工作台、组织与成员、历史证书，页面不含下级管理员角色或相应控制项。
- 归档历史证书已完成真实下载验收。为不改变原有赛事的 current 状态，使用唯一 ID 的临时数据库 fixture 建立一个已发布、非当前赛事及其赛项/公开资料；临时普通用户通过产品 API 注册、登录并报名，管理员再通过产品 API 审核报名（200）、上传 PNG 证书（201）、发布证书（200）和归档赛事（200）。
- 真实 Chrome 以该普通用户登录，在“历史证书”按归档赛事查询到 1 条证书并实际点击下载。授权文件复核为 HTTP 200、`image/png`、70 字节；页面只有查询和下载控件，没有发布、上传、删除或编辑控件，满足归档界面只读。
- 临时赛事的产品删除接口返回 500，未将其误记为成功；随即按该唯一赛事 ID 直接以子表到赛事的数据库事务删除证书、报名、赛项、公开资料和赛事，提交后复核 fixture 事件/报名/证书均为 0，受限证书目录文件数为 0。该直接删除仅用于回滚本次数据库 fixture，未触碰原有赛事或内容。
- 上一轮“32 项”汇总不再作为归档下载验收证据；独立复跑的相关自动化结果为 `App.test.js` 22 项和访问控制 API 1 项，但本节结论以实际浏览器下载为准。
- 此轮完成后再次按预览和确认串清理临时用户和审计数据，再从服务器权限 `0600` 的凭据文件经 stdin 恢复唯一管理员。最终远程 smoke 再次全部通过；最终计数为用户 1、组织 0、成员 0、赛事参与 0、报名 0、结果 0、证书 0、导入批次 0、审计日志 0，赛事 4、赛项 12、内容 3；API、PostgreSQL、Web、Backup 均为 healthy。

## 归档赛事删除修复与部署

- 修复提交：`83740470cafad0b2c5e7b1f3dc6ba2043f97020e`。根因是彻底删除赛事时内存快照没有移除其公开赛事资料；物理文件清理后的第二次持久化会尝试写回该悬空资料，并在 PostgreSQL 外键约束下失败。修复在删除赛事前同步移除对应 `eventPublicProfiles` 行。
- 先添加回归测试：为非当前归档赛事加入公开资料和证书文件，断言产品 DELETE 后赛事、公开资料、赛项、报名、证书、导入资料及物理文件均不存在。修复前该断言实际失败（公开资料残留）；最小修复后聚焦资源清理测试 5/5 通过，API 全量测试 316/316 通过。
- 部署前重新创建并验证数据库备份 `backups/aerogp-20260730T063435Z.dump`、上传卷备份 `backups/uploads/aerogp-uploads-20260730T063435Z.tar.gz`、旧源码 `backups/source-before-delete-profile-fix-20260730T063435Z.tgz`，并保存同一时间戳的 API/Web 回滚镜像标签。候选源码预检通过；仅重建 API，四服务均保持 healthy。
- 生产回归：建立唯一 ID 的临时非当前归档赛事，包含公开赛事资料、赛项、报名及实际 PNG 证书文件。修复后的产品 DELETE 返回 HTTP 200；响应确认删除 1 个文件且无失败，数据库复核赛事/公开资料/赛项/报名/证书均为 0，PNG 文件不存在。
- 回归结束后按预览和确认串清理测试用户与审计数据，并从受限凭据文件经 stdin 恢复唯一管理员。服务器 `.release` 为修复提交完整 SHA；最终计数为 `1|0|0|0|0|0|0|0|0|4|12|3`，认证 smoke 全通过，API、PostgreSQL、Web、Backup 均为 healthy。

## 后续操作

- 如需查看管理员凭据，仅在受控终端执行：`ssh aerogp "cat /root/aerogp-admin-credentials.txt"`。
- 如发生需要回滚的故障，先保留证据，再使用本报告列出的源码备份或回滚镜像；默认保留 PostgreSQL 与上传卷。
